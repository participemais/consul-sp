<% provide :title do %>
  <%= "#{t("admin.header.title")} - #{t("admin.menu.legislation")}" %> -
  <%= "#{@plan.title} - #{t("admin.progress_bars.index.title")}" %>
<% end %>


<h2><%= @plan.title %></h2>

<%= render "/admin/open_gov/subnav", active: "monitoring" %>

<%= render "admin/progress_bars/progress_bars", progressable: @plan %>
