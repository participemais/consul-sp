<% provide :title do %>
  <%= "#{t("admin.header.title")} - #{t("admin.menu.legislation")}" %> -
  <%= "#{@process.title} - #{t("admin.legislation.milestones.index.title")}" %>
<% end %>

<%= back_link_to edition_legislation_processes_path, t("admin.legislation.processes.edit.back") %>

<h2><%= @process.title %></h2>

<%= render "edition/legislation/processes/subnav", process: @process, active: "milestones" %>
<%= render "summary_form", process: @process %>
<%= render "edition/milestones/milestones", milestoneable: @process %>
