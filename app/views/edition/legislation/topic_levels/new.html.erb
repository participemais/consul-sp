<% provide :title do %>
  <%= t("admin.header.title") %> - <%= t("admin.menu.legislation") %> - <%= @process.title %> - <%= t("admin.legislation.topics.index.title") %>
<% end %>

<div class="legislation-admin legislation-draft-versions-index">

  <%= back_link_to(
    document_edition_legislation_process_topics_path, t("shared.back")
  ) %>

  <h2><%= @process.title %></h2>

  <%= render "edition/legislation/processes/subnav",
    process: @process, active: "topics"
  %>

  <h2 class="inline-block">
    <%= t("admin.legislation.topic_levels.new.title") %>
  </h2>

  <%= render 'form',
    url: edition_legislation_process_topic_levels_path(@process)
  %>

</div>
