<% provide :title do %>
  <%= t("admin.header.title") %> - <%= t("admin.menu.legislation") %> - <%= @process.title %> - <%= t("admin.legislation.questions.index.title") %> - <%= t("admin.legislation.questions.new.title") %>
<% end %>

<div class="legislation-questions-new legislation-questions-form">

  <%= back_link_to edition_legislation_process_questions_path(@process), t("admin.legislation.questions.new.back") %>

  <h2><%= @process.title %></h2>

  <%= render "edition/legislation/processes/subnav", process: @process, active: "questions" %>
  <div class="row">
    <div class="small-12 column">
      <h3><%= t("admin.legislation.questions.new.title") %></h3>
    </div>
  </div>

  <%= render "form", url: edition_legislation_process_questions_path(@process) %>
</div>
